# Data

Place nominal `.fjs` files in separate train, validation and test directories.
Use them with `train_ramp.py --train-dir ... --validation-dir ...
--test-dir ...`; health overlays are generated by the training entry point.
